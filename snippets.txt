# Create symlink
ln -s /target /link

# Simulator
xcrun simctl openurl booted

# Swift
1 / UIScreen.mainScreen().scale
if case let Enum.case(param, _) = m { ... }

# LLDB
p String(data: data!, encoding: .utf8) 

# Resize icon
rsvg-convert -h 32 icon.png > "icon$i.png"

# Git
git branch | grep -v "master" | xargs git branch -D
git rebase -i --root

