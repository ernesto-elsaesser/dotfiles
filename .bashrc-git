# start git shell via
# > bash --rcfile=.bashrc-git

# bash completion
source /etc/profile

# prompt
PS1="\w ($(git branch --show-current) $(git diff --numstat | awk '{c+=1}{i+=$1}{d+=$2} END{print c"C +"i"/-"d}' )) > "

MSG_FILE="$HOME/.commit_msg"

alias s='git status -s'
alias a='git add --all; s'
alias m='echo > $MSG_FILE'
alias c='git commit -F $MSG_FILE'
alias p='git push; rm $MSG_FILE'

alias pc='c; p'
alias ca='a; c'
alias pa='a; c; p'

alias pl='git pull'
alias pu='git push -u origin HEAD'
alias mr='git merge --no-edit'
alias co='git checkout'
alias lo='git log --pretty=oneline'
alias cb='git checkout -b'
alias bd='git branch -d'
alias rb='git rebase'
alias rh='git reset --hard'

